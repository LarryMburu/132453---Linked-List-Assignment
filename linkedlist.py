class Node:
    def __init__(self, data):
        self.data = data
        self.next = None

so first thing first we have to create a representation for each node on the list. This is done using the data which stores the value of the data
class LinkedList:

